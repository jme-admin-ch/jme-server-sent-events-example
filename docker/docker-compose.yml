---
services:
  postgresql-db-local:
    image: postgres:18.4
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=db-user
      - POSTGRES_PASSWORD=db-pass
      - POSTGRES_DB=sample-db

  postgresql-secure-example-db-local:
    image: postgres:18.4
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_USER=db-user
      - POSTGRES_PASSWORD=db-pass
      - POSTGRES_DB=sample-secure-example-db


  broker:
    image: confluentinc/cp-kafka:8.3.1
    hostname: broker
    ports:
      - "9092:9092"
    volumes:
      - ./kafka_server_jaas.conf:/etc/kafka/kafka_server_jaas.conf
    environment:
      # KRaft mode settings
      KAFKA_BROKER_ID: 1
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: 'broker,controller'
      KAFKA_CONTROLLER_QUORUM_VOTERS: '1@broker:29093'
      # KRaft cluster ID - format properly, a 22-character base64-encoded string, for the confluent platform
      CLUSTER_ID: 'MkU3OEVBNTcwNTJENDM2Qk'
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:SASL_PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://broker:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9092,CONTROLLER://0.0.0.0:29093
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      # Authentication
      KAFKA_OPTS: "-Djava.security.auth.login.config=/etc/kafka/kafka_server_jaas.conf"
      KAFKA_SASL_ENABLED_MECHANISMS: SCRAM-SHA-512
      # Temporary storage settings
      KAFKA_LOG_DIRS: /tmp/kafka-logs
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    healthcheck:
      test: ["CMD", "kafka-topics", "--bootstrap-server", "broker:29092", "--list"]
      interval: 5s
      timeout: 5s
      retries: 20

  broker_init:
    image: confluentinc/cp-kafka:8.3.1
    command: "kafka-configs --bootstrap-server broker:29092 --alter --add-config 'SCRAM-SHA-512=[password=user-secret]' --entity-type users --entity-name user"
    depends_on:
      broker:
        condition: service_healthy

  kafka-init:
    image: confluentinc/cp-kafka:8.3.1
    depends_on:
      broker:
        condition: service_healthy
      broker_init:
        condition: service_completed_successfully
    entrypoint: ["/bin/sh", "-c"]
    command: |
      "
      # Wait for Kafka to accept admin requests
      until kafka-topics --bootstrap-server broker:29092 --list >/dev/null 2>&1; do sleep 1; done
      # Create topics
      kafka-topics --bootstrap-server broker:29092 --create --if-not-exists --topic jme-server-sent-events-scs-notifyclient --partitions 1 --replication-factor 1
      kafka-topics --bootstrap-server broker:29092 --create --if-not-exists --topic jme-server-sent-events-secure-scs-notifyclient --partitions 1 --replication-factor 1
      "

  schema-registry:
    image: confluentinc/cp-schema-registry:8.3.1
    hostname: schema-registry
    depends_on:
      broker:
        condition: service_healthy
      broker_init:
        condition: service_completed_successfully
    ports:
      - "7781:8081"
    environment:
      SCHEMA_REGISTRY_HOST_NAME: schema-registry
      SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS: PLAINTEXT://broker:29092
