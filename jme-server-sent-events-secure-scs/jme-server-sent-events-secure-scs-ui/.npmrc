# jest-preset-angular@14 declares a peer dep on jest@^29, but @angular-builders/jest@20
# bundles it internally and it works correctly with jest@^30 at runtime.
# legacy-peer-deps avoids the false-positive resolution error on plain npm install.
legacy-peer-deps=true
