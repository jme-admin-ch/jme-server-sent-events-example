<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.admin.bit.jeap</groupId>
        <artifactId>jeap-spring-boot-parent</artifactId>
        <version>38.0.1</version>
        <relativePath/>
    </parent>

    <groupId>ch.admin.bit.jeap</groupId>
    <artifactId>jme-server-sent-events-example</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <description>
        Sample project for server sent events (SSE) using the JEAP framework.
    </description>

    <properties>
        <jeap-oauth-mock-server.version>8.0.1</jeap-oauth-mock-server.version>
        <java-uuid-generator.version>5.2.0</java-uuid-generator.version>
        <playwright.version>1.62.0</playwright.version>
    </properties>

    <modules>
        <module>jme-server-sent-events-scs</module>
        <module>jme-server-sent-events-secure-scs</module>
        <module>jme-server-sent-events-auth-scs</module>
    </modules>

</project>
